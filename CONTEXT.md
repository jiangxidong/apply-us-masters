# 美国授课硕申请

这套 agent skills 的领域词汇表。域是**美国 coursework master（授课型硕士）申请**：从选校走到网申准备包、**停在提交前**，全部进度持久化在一个跨会话的工作区里。

本文件**只定义词**：词的含义、中文名与 ASCII 标识符的对应、词与词之间的边界。文件长什么样归 [状态层的文件契约](https://github.com/jiangxidong/EduApplication/issues/4)，表怎么排版、措辞怎么写归 [选校推荐的输出契约](https://github.com/jiangxidong/EduApplication/issues/11)。

🔴 **两条判据决定一行字该不该在这里**（[ADR 0011](docs/adr/0011-the-glossary-defines-words-the-contract-holds-the-values.md)）。原来这里写的是「实现细节一律不进这里」，而「实现细节」不可判定——[#23](https://github.com/jiangxidong/EduApplication/issues/23) 正是照着它把整张归属表写了进来。

1. **它为了保持为真，需不需要在别处改动时被改写？** 需要 → 它是别处的**镜像**，不写在这里。（这是状态层契约判工作区文件用的同一台机器，抬一层用在 repo 文档上。）
2. **agent 在什么时刻读它？** 命名领域概念时 → **这里**；决定一条事实落进哪个文件、哪个节、谁能写 → **状态层契约**。

由此两条禁令：本文件**不出现 owner / 阶段列**；引用别处的枚举时**不写全**，只举用得着的那几项并给出指针——写全了它就变成第二个权威，看起来无害，实际就是漂移的起点。

> **状态层契约当前住在哪**：文件是 `prototypes/state-workspace-v0/CONTRACT.md`，在 **`prototype/state-layer` 分支，尚未合并进 `main`**——所以本文件里指向它的路径链接在 `main` 上是死的，这里明写出来而不是假装。在 `main` 上可达的权威入口是 [#4](https://github.com/jiangxidong/EduApplication/issues/4) 的回填评论。

## 三重身份

有落盘形态的术语，**中文名**（展示层，给用户看）与 **ASCII 标识符**（落盘层，被脚本聚合）必须分开写。没有落盘形态的，标识符写 `—`。

> 🔴 中文名**永不落进被聚合的列**：macOS 的 `uniq` 在中文下会数错、`sort -u` 会直接丢行，且与文件格式无关（[#4](https://github.com/jiangxidong/EduApplication/issues/4) §4.5 实测）。
>
> 🔴 **同类第二条**（[#8](https://github.com/jiangxidong/EduApplication/issues/8) 实测）：脚本里凡在中文文案中引用 shell 变量，**一律写 `${var}` 不写 `$var`**——`bash` 会把紧跟其后的中文标点的首字节吞进变量名，`"$got）"` 报 `got<乱码>: unbound variable`。两条都是中文内容撞 shell 工具，且都只在跑起来时才暴露。

---

## 工作区

**工作区** — `workspace`
一个申请人全部申请状态的**唯一权威目录**，由用户在会话开始时指定——产品不选路径。跨会话、跨设备续得上，全押在它上面。
_Avoid_: 项目目录、申请文件夹

**申请季** — `season`（形如 `2027fall`）
这一轮申请瞄准的入学学期。一个工作区**同时只属于一个申请季**。

**换季** — `—`
把工作区的 `season` 改到下一季这个动作。二申用户**不新开工作区**——画像、素材、旧文书、推荐人关系全部可复用，重开等于把核心资产扔掉。
换季只做两件事，**不清空任何内容**：全表证据降级（见「降级」）与状态回退（见「状态」）。

**首轮契约** — `—`
用户第一次说话之后，产品在第一屏承诺做到与承诺不做的事（[#15](https://github.com/jiangxidong/EduApplication/issues/15)）。

🔴 **首轮草案是一面镜子，不是一份清单**（[ADR 0012](docs/adr/0012-the-first-reply-is-a-mirror-not-a-list.md)）：首轮给出的是**申请人自己这一侧**（从他带来的材料里抽出的画像骨架与主张初稿），**不是**「你该申哪些学校」。理由是取证纪律——冷启动时本会话零取回，任何学校事实都只能凭记忆，撞停手线取证类。
_Avoid_: 起步清单、推荐方向、首屏选校建议

**两拍** — `—`
首屏的节奏：第一拍**建制**（在哪个目录初始化 + `season`，一次带默认值的确认），第二拍**内容**（第 0 问 + 抽取草案）。
「只问一个问题」防的是**内容侧**的 20 问综合症；建制侧那两件事不是审问，是不答就写不出 `apply.md` 的物理前提，而且用户答完立刻看到工作区被建出来。

**第 0 问** — `—`
第二拍开头的那一问：**「你手上现在有什么材料」**。
准入判据：**这一问的答案会立刻改变产品下一个可取证的动作。** 按此判据，`euro-grad-apply` 的「毕业目标」在本产品**不作第 0 问**（见「毕业去向意向」）。
它的可选答案里**明写**一条「我还不知道要申什么」——兜底是一个**摆出来的选项**，不是一个 agent 暗中判定的分支。判定用户是否茫然没有判据，同 [ADR 0008](docs/adr/0008-the-owner-binds-to-a-section-not-a-file.md) 否掉「时序」判据的理由：它循环。

**逐文件交接** — `—`
冷启动读取用户既有材料的**唯一**方式：用户一次给一个文件并声明它是什么，agent 按声明对应的槽位敏感级决定读不读。**绝不扫描目录、绝不按扩展名猜。**
存在的理由：「先抽后问」要求读，「槽位先于文件」要求读之前就知道敏感级——而冷启动时工作区还不存在，简历不在任何槽位里，两条硬碰。逐文件交接把「读之前就知道」的载体从**路径**换成**用户声明**，两条同时成立，且 agent 从不扫描 = 误读的物理前提不成立。
声明只用来**决定读不读**，**不用来分类落盘**（落盘仍走槽位规则）。只要用户的话里出现身份类线索（绿卡、I-20、护照、身份证），无论声明成什么**一律不读**。

**首屏位次** — `—`
🔴 **git 检测必须先于第一次写盘。** [#17](https://github.com/jiangxidong/EduApplication/issues/17) 只定了「会话启动必检测」，没定它与写盘的先后；顺序反了这条防御就废了——工作区已被 git 化时，先建 `apply.md` / `profile.md` 再检测，那两个文件已经躺在那里等着被 `git add .` 一把捞走。检测的全部价值在于**在有东西可被误 push 之前**告知。

**续上** — `—`
入口的第二个用途：工作区已存在时接上上一次。
判断「该去哪」时**两样并排摆出、由用户选**：`log.md` 的「下一步 / 卡在哪」（历史意图）与现算一次派生视图（当前事实）。
⚠️ 这**不是** [ADR 0007](docs/adr/0007-a-checkmark-is-earned-by-a-fetch-not-by-a-capability.md) 一线否掉的那个循环论证——那里否的是拿 agent 自己的日志**验证 agent 自己的行为**（取证），这里是拿日志**接续意图**，而意图本来就只有日志记着，派生视图算不出来。但正因为它可能是三个月前的意图，不能单独用。两样不一致时，**不一致本身就是信息**（日志说「下一步核实 deadline」而派生视图显示 deadline 已全绿 = 有人手动改过）。

---

## 申请人

**申请人画像** — `profile`
申请人**结构化的、会被反复引用的、稳定的输入**：姓名拼写、学历、成绩、语言分、工作经历、目标与预算。既含**事实**（GPA、语言分），也含**约束与偏好**（预算、毕业去向意向）。
判别式：**它会被别的阶段直接引用，且改一次就要处处生效。**
⚠️ 「能填进网申表单的格子」是画像**最常见的形态**，**不是判别式**——预算与毕业去向意向填不进任何格子，却是画像的一等成员（[#15](https://github.com/jiangxidong/EduApplication/issues/15)）。这一刀与 [#22](https://github.com/jiangxidong/EduApplication/issues/22) 对「硬门槛是分档唯一合法的锚」做的是同一刀：**枚举与判别式打架时，改判别式，不删枚举。**
_不包含_：文书素材、文书草稿、成绩单扫描件——那些是叙事与原件，形态和用途都不同。
_Avoid_: 个人资料、背景、简历

**毕业去向意向** — `post_grad_intent`
申请人对毕业后去向的当前倾向。四值 ASCII 枚举：

| 中文 | 标识符 | 含义 |
|---|---|---|
| 留美 | `us` | 倾向毕业后留在美国 |
| 回国 | `home` | 倾向毕业后回国 |
| 都行 | `either` | **已经想过，两边都接受** |
| 还没想 | `undecided` | **还没有答案** |

⚠️ `either` 与 `undecided` **必须分开**，切分同 [ADR 0009](docs/adr/0009-safer-is-defined-by-the-programs-second-published-line.md) 之后 `undecided` / `ineligible` 那一刀：**已经有答案** vs **还没有答案**。压成一个值，会把「先留两年再看」这个最常见的真实答案错判成茫然，直接把用户推进兜底路径。

🔴 **它不是第 0 问**（[#15](https://github.com/jiangxidong/EduApplication/issues/15)）。`euro-grad-apply` 把「毕业目标」设为强制第 0 步，本产品**不照抄**：第 0 问的准入是「答案会立刻改变产品下一个**可取证**动作」，而本字段两支都过不了这一关——「回国」那一支要的「国内认可度」已被 [#16](https://github.com/jiangxidong/EduApplication/issues/16) 明确排除（社区讨论密度无可链接出处）；「留美」那一支要的 STEM 指定 / CIP code **已由 [#34](https://github.com/jiangxidong/EduApplication/issues/34) 查实：能取证，但取不全**。

🔴 **本字段当前不驱动任何筛选或排序，只作为画像记录留存**（[#34](https://github.com/jiangxidong/EduApplication/issues/34)）。三所样例只有 UIUC 一所在申请阶段可取回（大学层 Program Inventory 逐项目给 CIP），Columbia 与 Cornell 的公开面根本不公布——两校 international office 的官方口径都是「见**录取后**的 I-20 第 1 页」，而申请阶段的人手上没有那份文件。用三分之二为空的属性去过滤项目池，产出的不是决策是噪音。
⚠️ **不要再查一遍**：逐层取证记录在 `research/stem-cip` 分支的 `docs/research/stem-cip-designation.md`（`0c36952`）。
它落 ASCII 枚举而非中文自由文本仍然值得：UIUC 那条链两环各有各的 `✓`（大学 Program Inventory → CIP，DHS STEM 名单 → 该 CIP 在册），覆盖率一旦补上，它当场变成会被机械读取的字段。

**就读院校** — `institution_id`（ASCII）
申请人读过的一所学校（本科 / 交换 / 辅修各算一所）。它是继 `program_key` / `channel_key` / `recommender_id` 之后的**第四个主键**，但**定义住在 `profile.md` 的学历条目里**——`documents/` 的目录名只是**引用**它，不另行定义。同一实体两处命名必漂移。
逐院校的槽位（`transcript` / `legend` / `translation` / `degree`）一律按它分组。中文校名**永不进目录名**（CJK 一旦被枚举就丢行）。

**主张** — `claim` / 主键 `claim_id`（形如 `c01`）
申请人希望招生官相信的、**关于他自己的一句断言**。
判别式：**它必须能被一条具体素材支撑或证伪。**
「我想读 CS 硕士」是**意图**不是主张；「贵校 X 教授的实验室做 Y」是**学校事实**，走证据标记那条线；「我抗压能力强」**是**主张——它可被素材支撑，只是现在没有，那正是缺口。

主张是**全局一等对象**，不是文书的局部产物：**文书是申请人自述主张，推荐信是第三方佐证同一批主张**。
全局只有一个主张集合，两条线都从它取——见 [ADR 0006](docs/adr/0006-claims-are-one-shared-truth-source.md)。文书是**从主张清单里选**，不是发明主张，因此全局集 ⊇ 任一篇文书用到的集合。
**「谁能证实」不是主张的属性**——那是素材的独立属性（见「具体素材」），主张层存一遍就是第二真相源。

**由谁来说** — `voice`（`self` / `referee` / `both`）
一条主张适合由谁陈述。比较性、评价性的断言（「他的工程直觉在同届里排前 5%」）申请人自己写进文书是失礼的，只能由推荐人说；动机类断言（「我为什么从后端转做 ML」）推荐人说不了。
它是**一个集合的一个属性，不是两个集合**——两个集合就是第二个真相源。

**推荐人** — `recommender_id`（形如 `r1` / `r2`）
为申请人署名作证的第三方。它是继 `program_key` / `channel_key` 之后的**第三个主键**（[#12](https://github.com/jiangxidong/EduApplication/issues/12) §4）。

值域是 **ASCII**，两条理由叠加：被聚合的列里不能出现中文（见「三重身份」的 🔴），且它同时要当文件名用。

粒度是**人，不是（人 × 项目）**——美国现实是推荐人写**一封**信、往 N 个 portal 各提交一次，按项目切会把同一份内容复制 N 份。逐（人 × 项目）的只有**提交事实**与逐校机制状态。

「这个人能证实什么」是推荐人的属性，与素材的**可验证性**配对使用。⚠️ 可验证性**在文书线是独立属性、不参与充分度判定，在推荐信线是判定本体**——一条无人可证的自学项目素材是合格的文书素材，**同时不是**合格的 support pack 素材。两条判据各自成立，不互相覆盖（[#12](https://github.com/jiangxidong/EduApplication/issues/12) 结案后修正 §3）。

**文书素材** — `material` / 主键 `material_id`（形如 `m01`）
一段可被文书引用的真实经历，是**主张的支撑物**。素材库是状态层的一等公民，不是文书的草稿纸。
`material_id` 是主键，因为 `claims.md` 的 `materials` 列按它做**跨文件引用**；它同时进文件名（`m01-<中文短名>.md`），好让引用能靠 glob 解析而不必打开每个文件读 frontmatter。
每条素材带**敏感**标记（`sensitive` / ASCII 二元，[#17](https://github.com/jiangxidong/EduApplication/issues/17)）：由 agent 提议、**用户可下调**。标 `yes` 的素材，其**原文永不直接进任何产物**——引用时须逐条确认「原文 → 拟用表述」。向用户本人复述不受此限。
每条素材的**可验证性**落成 `verifiable_by`（`recommender_id` 列表，[#38](https://github.com/jiangxidong/EduApplication/issues/38)）：**空列表 = 缺人缺口**，与 `claims.md` 的 `materials` 空 = 缺素材同构。「谁能证实」因此被限制成必须先是一个推荐人候选——联系不上属于推荐人的状态，不是素材的属性。
🔴 **素材的字段一律列名，不写「N 字段」**（[#30](https://github.com/jiangxidong/EduApplication/issues/30)）。「六字段 / 七字段」曾在三处票面上并存三种计数，删掉 `已用于` 会立刻产生第四种——同 [#24](https://github.com/jiangxidong/EduApplication/issues/24) 在文件上杀掉的病（「表的行数就是文件数，任何文件都不带序号」）。**v1 的字段名单归状态层契约**（[ADR 0011](docs/adr/0011-the-glossary-defines-words-the-contract-holds-the-values.md)：词汇表定义词，契约持有取值）。
🔴 **语义槽位不是落盘字段**（[ADR 0014](docs/adr/0014-a-semantic-slot-is-not-a-stored-field.md)）。#10 的六个是「一条素材必须答什么」的判据，**不是 frontmatter 键名**；frontmatter 只放**有落盘消费方**的键，其余留正文。此前两套词汇被当成同一件事的两个版本，于是 #27 把 frontmatter 现有的键读成了「通往 #10 名单的进度」。

**具体素材** — `—`
能答**三问**的素材：**什么时候** / **你做了什么动作** / **结果是什么**。三问答不全的是感想，不是素材。
⚠️ **「谁能证实」不在判据里**——可验证性是素材的独立属性（供推荐信选人用）。自学项目、个人作品无人可证，**照样是合格素材**。
⚠️ **「具体」没有落盘形态**（[ADR 0014](docs/adr/0014-a-semantic-slot-is-not-a-stored-field.md)）：三问住在正文，不设字段——一个 `concrete: true` 为保持为真必须跟着正文改，源就在同一个文件里。**判「具体」的是采集现场的 agent，不是 CI**；契约那条固定小标题的形状规则只判形状，三个标题齐全而底下写的是感想，脚本一样放行。

**素材门槛** — `—`
「不许有无素材支撑的主张」这条**质量硬约束**。单位是**主张**不是篇数；3–5 个是参考值，不是闸门。
门槛拦的是**成稿**，不是模式：素材不足时代写模式照样能进，只是交付**带缺口标注的骨架 + 缺口清单**，而不是成稿。
**作用域是「进了成稿的主张」，不是全局主张集**——全局集里允许存在零素材的主张，那正是缺口本身。一条 `materials` 为空的主张进不了任何成稿，但能进带缺口标注的骨架。
🔴 **这道门槛「过没过」是派生视图**——现算、绝不落盘（[#61](https://github.com/jiangxidong/EduApplication/issues/61)）。门槛本身是一条规则，规则写在这里；**落盘的是规则，不是它的判定结果**。

---

## 项目与渠道

**项目** — `program` / 主键 `program_key`
一个具体的、可申请的硕士项目，粒度到 `学校 · 学院 · 项目名`。**最小分叉粒度是项目，不是学校**——同校不同院的门槛、表单、AI 政策都可能不同。

**申请渠道** — `channel` / 主键 `channel_key`
共用同一套 portal、申请费与上传规则的**通道**，通常落在学院或研究生院这一层。它是**通道**，不是组织架构——Cornell 的渠道是研究生院，并不等于某个学院。
_Avoid_: 申请单元

**项目池** — `program pool`
申请人在考虑的**全部**项目，一行一个项目。它是**唯一的项目实体**——不存在与之并列的第二张「清单」表。
_Avoid_: 选校表、候选池

**选校清单** — `—`
项目池里 `status ∈ {shortlist, applying, submitted}` 的**派生视图**。不落盘。

**投递名单** — `—`
项目池里 `status ∈ {applying, submitted}` 的**派生视图**。不落盘。

**状态** — `status`
项目在池子里的处境。**唯一的状态轴**——不存在并行的「决定」列。

| 中文 | 标识符 | 含义 |
|---|---|---|
| 在看 | `considering` | 进了池子，还在了解 |
| 认真考虑 | `shortlist` | 在认真考虑，**还没拍板** |
| 已定投 | `applying` | **已拍板要投**，尚未提交 |
| 已提交 | `submitted` | 这一季已经交了 |
| 已排除 | `dropped` | 明确排除，**必须带一行理由** |

- `shortlist → applying` 的分界是**拍板「投」的那一刻**，不是坐下来填表的那一刻。「开始填表」不产生状态变化。
- 🔴 **`applying → submitted` 这一跳产品一个动作都没有**——「点提交」在停手线上。`submitted` 是**用户报告的一件已发生的事**，产品只负责记账；**「投递」不是一个阶段**（[#25](https://github.com/jiangxidong/EduApplication/issues/25)）。
- `dropped`（明确排除过）与「从没进过池子」是两回事。
- 只有 `dropped` 断言的是**持续判断**（「这个项目不适合我」），其余四个都在断言**本季动作**。换季时怎么处理由此推出，规则见 [ADR 0003](docs/adr/0003-reapplying-reuses-the-same-workspace.md)。

> 中文标签是展示层措辞，可改；ASCII 标识符是契约，不可随手改。

---

## 判断

**硬门槛** — `—`
项目**公开写明的**最低准入条件：最低 GPA、语言分下限、先修课、是否要 GRE。它是可核实的事实，因此是分档**最常见**的锚——但**不是唯一的锚**——`tier_basis` 的准入以「依据」词条的测试为准。Columbia CS 明写 `no minimum GPA` 却公布录取群体分布，照旧可分档。

**第二条线** — `—`
项目在**最低线之外**公布的第二个关于申请人条件的数字。它是 `safer` 判据里「明显」二字的**唯一合法替身**——标定权在学校，不在本产品（[ADR 0009](docs/adr/0009-safer-is-defined-by-the-programs-second-published-line.md)）。分两类，判别式是**这句话在描述「谁有资格申请」还是「谁被录取了」**：

| 类型 | 触发措辞 | 描述的是 | 跨过它得到 |
|---|---|---|---|
| **A · 更高的公开软门槛** | `recommended` / `preferred` / `expected` / GRE waiver 门槛 / conditional admission 分界 | **资格** | `safer` |
| **B · 录取群体分布** | `typically` / `average` / `median` / `middle 50%` | **录取结果** | 落在其中 = `match`；越过**公布区间的上界** = `safer` |

⚠️ 触发措辞只判定第二条线的**类型**，**不判定一条线是不是「第二条」**——那取决于项目是否另有更低的最低线。UT Dallas 的 `"3.0 or better is expected"` 用了类型 A 措辞，但它是该项目**唯一**的线 → 无第二条线可跨。
⚠️ **措辞与实质冲突时按实质判**——触发措辞是**线索**，不是判据。类型的定义是语义的（这句话在描述「谁有资格申请」还是「谁被录取了」），不是词表的。UIUC Siebel School 的项目对照表列头写 `Undergraduate GPA (Recommended, 4.0 scale)`（类型 A 措辞），但学校**自己的 FAQ 给这个数字下了定义**：`Our website states the average GPA of admitted students. It is not a minimum.` → 描述的是录取结果，**判类型 B**（[#45](https://github.com/jiangxidong/EduApplication/issues/45) 实测）。代价是实打实的：读成类型 A，跨过 3.2 就得出 `safer`；读成类型 B，正确答案是 `match`。**见到 `recommended` / `preferred` 时，再查一次学校有没有在别处解释过这个数字。**
⚠️ 类型 B 通往 `safer` **必须有离散度**（公布区间）；只给单个中位数或下界时**不判** `safer`。跨过「录取者典型下界」只证明你和大多数被录的人一样，那是 `match` 的定义。
_Avoid_: 明显超过、余量、安全线

**擦线** — `—`
一个维度上，**该维度有第二条线且你没跨过**。只有一条线的维度**不存在擦线**——达标就是达标。
存在的理由：不这么定义，「擦线」就是「明显」的同一个病（主观余量语言）。这样它是可机械判断的谓词，且不引入任何新数字。
_Avoid_: 险过、勉强达标、卡线

**分档** — `tier`
一个**顾问判断**，永远不带证据标记。**五档**，前四档以硬门槛为锚：

| 中文 | 标识符 | 断言的是 |
|---|---|---|
| 冲刺 | `reach` | 你满足硬门槛，但拒你的主要原因会是**别的申请人比你更强**，而这个比较所需的数据不公开 |
| 匹配 | `match` | 你满足硬门槛，且有**公开信号**表明你落在它录取群体的中段 |
| 较稳 | `safer` | 你**跨过了该项目公布的第二条线**，拒你的主要风险**不是你的条件** |
| 待判 | `undecided` | 上述三条都写不出依据 |
| 未达门槛 | `ineligible` | 该项目**全部公开维度中至少一个已取证未达标** |

⚠️ `safer` **从不断言「会录」**，只断言「拒你的理由不会是你的条件」。凑不齐的档**不凑**。
⚠️ `safer` 的完整判据是一个**合取**：① 全部公开维度达标 ② 至少一个维度跨过**第二条线**（类型 B 须越过公布区间的上界）③ 没有任何维度**擦线**。无数值的二元维度（先修课修过/没修过、本科院校国别形态）**只能挡不能立**——它能否掉 `safer`，永远确立不了 `safer`。
⚠️ **`safer` 是项目属性先行的判断**：项目不公布第二条线，任何申请人在它这里都拿不到 `safer`，**与你多强无关**。此时 `reach` 是**终态**，不是「暂时没查够」。
⚠️ `undecided` 与 `ineligible` 的切分：**「还没考托福」= `undecided`**（不知道你是否满足）；**「考了 95、要求 100」= `ineligible`**（知道你不满足）。`ineligible` 是**事实轴**，与 `status=dropped`（**决定轴**）无关——硬申一个差 0.3 的项目是合法选择。
⚠️ `ineligible` 行的 `tier_void_if` **方向与其余四档相反**（作废条件是好消息：「托福达到 100」）。🔴 它只能说「就**不再是** `ineligible`」，**不能说「就够了 / 就稳了」**——跨过最低线只是回到 `reach` 的起点，升到哪一档要重跑上面的合取。
⚠️ `ineligible` **不给阻断权**：用户把它切到 `status=applying` 时**只在那一刻告知一次**，不设闸门。
「什么证据能合法支撑一个分档」归 [分档的「依据」到底能填什么](https://github.com/jiangxidong/EduApplication/issues/16)；本表只定义每一档**在断言什么**。
_Avoid_: 保底、稳了、safe、不合格、够不着

**依据** — `tier_basis`
一条分档判断**点名的支撑事实**。它不是新的证据形态，是一条**指向已有 `✓` 事实的指针**（[ADR 0005](docs/adr/0005-basis-points-at-an-existing-checked-fact.md)）。

准入测试：一个信号能进 `tier_basis`，**当且仅当**它已经是、或能被写成 `channels/` 里一条带 `✓ <url>` 的事实行，或项目池的 `deadline` 列。判别式因此不靠人判断，而是**它有没有一行带 `✓` 的家**。够格的信号是一张**封闭白名单**（硬门槛、录取群体分布、项目公布的录取率……全表见 [#16](https://github.com/jiangxidong/EduApplication/issues/16) §3）。申请人自己的背景**算**依据——三档全是「你 vs 项目公布的线」的比较，不带画像根本写不出依据；表述只写「你的 X 与该项目公布的 Y 的关系」，不滑向录取概率。

⚠️ **这一格装的比「依据」多**：`tier_basis` = 至少一条依据 + 可选的**放大器**从句。放大器**不计入「至少一条」的计数**，也不得单独成立。
⚠️ 此前这里还有第三种从句（只在 `pseudo_safer=yes` 行上合法的 `经验：`）。**它已被删除**——伪保底的两半现在都要求可链接事实（见「伪保底」、[ADR 0015](docs/adr/0015-pseudo-safer-annotates-the-users-prior-not-the-tier.md)）。`tier_basis` 里**不存在任何零出处从句**。

- **不写 URL**：一句话，禁 `|`，禁换行。依据格只**点名**事实（「最低 3.0、项目建议 3.2、你 3.6」），URL 住在 `channels/` 那条事实行上——依据与出处物理分离，由 `✓` 连接。
- 两条机械判别式：`tier_basis` 空 ⇒ `tier` 必须是 `undecided`；`tier = ineligible` ⇒ `tier_basis` **非空**（未达标的行恰恰填得满）。
- 「点名的那条事实真在 `channels/` 里有 `✓`」是 **agent 侧纪律，无法机械校验**——不假装它能。
- 它强制了顺序：想给一个项目分档，必须**先把支撑事实收集进 `channels/`**。分档是收集事实的副产品，不是它的替代品——这正是「凭经验」最难混进来的地方。
_Avoid_: 匹配理由（那是「依据」+「作废条件」的渲染，不是第三个存储位）、凭经验（**任何形态**——`经验：` 从句那条例外已随 [ADR 0015](docs/adr/0015-pseudo-safer-annotates-the-users-prior-not-the-tier.md) 删除）

**放大器** — `—`
可链接、但**只能附着在依据上、不得单独成立**的信号。它能让一条分档判断更冲刺，撑不起这条判断本身——典型如「是否 rolling」：那是**时序**事实，不是**选择性**事实。够格的信号同样是封闭清单，见 [#16](https://github.com/jiangxidong/EduApplication/issues/16) §3。

🔴 **放大器不加任何存储标记。**「不得单独成立」是**写作规则**，不是标记——给它加标记等于在二元证据体系外造第三类值。

**作废条件** — `tier_void_if`
什么情况会让这条分档判断**不再成立**。一句话，禁 `|`，禁换行，**必填**——与「依据」同受「写不满就不写」约束。

定义是**中性**的，方向随档而变：前四档的作废条件是坏消息，`ineligible` 行是好消息（见「分档」的两条 ⚠️）。
⚠️ **承重不均**：它在 `safer` 行上做真功；`reach` 是众数档，其作废条件多为可推导的样板。**样板不是缺陷，仍必填**——它让「这条分档什么时候失效」跨会话保持显式。但别把它当 `reach` 的质量保证，`reach` 的质量保证是「依据」的准入测试。
⚠️ **它只对分档负责。** 伪保底**不借用这一格**——它的作废是可推导的（[ADR 0015](docs/adr/0015-pseudo-safer-annotates-the-users-prior-not-the-tier.md) 推翻了 [#11](https://github.com/jiangxidong/EduApplication/issues/11) 第三次更正的「复用」那半句）。
⚠️ 它与**失效传播**不是一回事：作废条件说的是**这条判断**在什么条件下不成立；失效传播是画像三字段（GPA / 本科院校 / 语言分）变更时，**只降**实际引用了该字段的行、自动降为 `undecided` 且**原句不保留**（[#16](https://github.com/jiangxidong/EduApplication/issues/16) §7）。

**伪保底** — `pseudo_safer`（`yes` / `no` / `unknown`）
一个**纯项目属性**的反直觉标注：这个项目**名义门槛低**（低到极致就是明写 `no minimum GPA`），**方向却极挤**。

它纠正的是**用户的先验**（「排名不高 = 保底」），**不是产品的分档**——分档那边早已判成 `reach`（项目不公布第二条线，谁都拿不到 `safer`）。典型形态：同一句话里既写 `no minimum GPA` 又写 `the program is highly selective`（Columbia CS MS，[#22](https://github.com/jiangxidong/EduApplication/issues/22) §3 实测表）。

- **两半各自要有 `✓` 事实行**：「名义门槛低」与「方向极挤」都住 `channels/<channel_key>.md`，各带各的 `✓ <url>`。写不满就填 `unknown`——**禁零出处的伪保底断言**（[#11](https://github.com/jiangxidong/EduApplication/issues/11) §3）。美国学校常常自己把「极挤」写出来（`highly selective` / `competitive`），所以这一半在美国范围内通常查得到。
- **不占「依据」也不占「作废条件」**：它不是分档判断的一部分，两半都不进「依据」的封闭白名单。作废是**可推导**的——支撑它的任一条 `✓` 事实变了就重判，同「缺口」那条判别式：可现算的东西不落盘。
- **不参与失效传播**：两半都在项目侧、零申请人输入，所以画像三字段变更（[#16](https://github.com/jiangxidong/EduApplication/issues/16) §7 链一）**永不触发它**。它与 `safer` 同属项目属性先行，方向相反——`safer` 由项目**公布了第二条线**给出，伪保底由项目**没有第二条线却又极挤**这个组合给出。
- 渲染成顾问判断节里一条**独立提示**，**不占分档表的格**（[ADR 0015](docs/adr/0015-pseudo-safer-annotates-the-users-prior-not-the-tier.md)；先例见 `prototype/program-shortlist` 原型）。挤进分档表，它就变回了一个分档修正词。

⚠️ **机械判别式**：`pseudo_safer = yes` ⇒ `tier ≠ safer`。伪保底**永远不推翻**一个由第二条线得来的 `safer`——那等于让顾问经验压过学校公布的数据，正是 [ADR 0009](docs/adr/0009-safer-is-defined-by-the-programs-second-published-line.md) 把标定权交给学校时堵上的洞。
⚠️ **`no` 的意思是「已经看过两半信号、判定它不是伪保底」**，不是「默认不是」。默认值是 `unknown`；把没看过的行填 `no`，这一列当场作废。
_Avoid_: 假保底（同一概念的旧名，中文名以本条为准）、保底（见「分档」）

**录取率** — `—`
学校或项目**公布**的历史录取比例。它是**事实**，可标 `✓`，但**必须带口径**（哪一年、全体还是国际生、含不含 waitlist）。查不到口径就不裸给数字。

**录取概率** — `—`
「你有 N% 概率被录」。**不做**——任何形态、任何位置都不输出。

---

## 证据

**证据标记** — `evidence`
一个事实是否经过官网核实，**二元**：

- `✓ <官网链接>` — 已核实，事实取自这个 URL
- `待核实` — 其余一切情况

`✓` 断言的是「**这个事实取自这一页**」，**不是**「agent 亲自看过这一页」。因此它有**两条合法来源，仅此两条**（[#20](https://github.com/jiangxidong/EduApplication/issues/20)）：agent **取回**过该页，或**用户提供**了该事实与该链接。用户自己查、自己贴链接，照样打 `✓`——责任随之落在用户身上。agent 凭记忆写出的 `✓` 行不合法（见「停手线」取证类）。

**取回** — `—`
把某个 URL 的**页面正文**取进上下文这个动作。它是 `✓` 的准入动作。
⚠️ **搜索结果摘要不是取回**——摘要可能来自索引快照而非官网现况，认它就等于认「凭记忆」。
粒度挂**页面**不挂事实：一次取回覆盖该页正文里的全部事实。**跨会话不继承**——上一会话取回过不算数。
⚠️ 「跨会话不继承」说的是**取回这个动作**不跨会话，**不是已落盘的 `✓` 行跨会话失效**——已经写下的 `✓` 只由**换季降级**改写（见「降级」），会话边界不动它。
取回**失败**（离线、工具被拒批、运行时无外部访问、PDF / 动态页取不到）与取回**成功但正文里没有这条事实**是两回事：前者是 `待核实`（无原因后缀，下一步「去官网查」），后者是 `待核实（官网未列）`。两者给用户的下一步相反，**agent 不得把取回失败说成「官网没写」**。

**待核实后缀** — `—`
`待核实` 括号里的补充说明。**共两种，可同时出现**，都不参与机械汇总（`待核实` 依然全中），只在生成自查清单时变成行动提示。

**① 原因后缀**——说明**为什么还没核实**。「我没查」和「查了官网就是不写」对应的下一步完全不同。取自**封闭词表**，不是自由散文：

| 后缀 | 含义 | 用户的下一步 |
|---|---|---|
| （无） | 还没查 | 去官网查 |
| `官网未列` | 查过了，官网不写 | 查也没用，发邮件问 admissions |
| `口径不清` | 查到了，但年份/群体/含义不明 | 先确认口径才能用 |
| `需邮件确认` | 只能通过联系院校得到 | 直接发邮件 |

**② 降级后缀**——**唯一形态**、参数化：`<上一季> 核过：<链接>`。它不属于封闭词表，因为它携带的是数据而非分类。

两者共存时**原因在前、降级在后，分号分隔**：`待核实（官网未列；2026fall 核过：<链接>）`。这不是边角情形——一个上季标 `✓` 的行降级后被重查、发现官网已不再列出，就会走到这里。

**降级** — `—`
换季时把 `✓ <链接>` 变成带**降级后缀**的 `待核实`。**必须保留上季链接**——抹掉就把「重新核实」变成「重新从零查」。

**惰性执行**：换季只改 `apply.md` 的 `season`。各阶段下次进入时，按 frontmatter 的 **`season_downgraded`** 表比对**自己那一行**，落后于当前 `season` 才就地降级自己的节，并戳新自己那一行。
由入口阶段一次性全表降级会构成**跨 owner 的写**（`programs.md` 与 `channels/` 的十个节分属三个阶段）。顺带：未被访问的节保持上季标记，比假装重查过更诚实。
🔴 **戳的粒度必须与 owner 绑定的粒度一致，不得挂在文件上**（[ADR 0008](docs/adr/0008-the-owner-binds-to-a-section-not-a-file.md) 限定 4，[#37](https://github.com/jiangxidong/EduApplication/issues/37)）。原先的**文件级 `season` 字段已删除**：一个 `channels/*.md` 有五个阶段 owner，谁先进来把文件 `season` 刷成本季，其余四个阶段就**永远不匹配失败、永远不降级**，陈旧被永久掩盖。**缺行 = 陈旧**（owner 集合由契约推出，不从文件读，所以缺席只有一种读法）；连带**建节即戳、降级即戳**，否则刚取回的 `✓` 会被判成陈旧。
渲染时读到尚未降级的 `✓`，**按该条事实所属 owner 的那一行比对，不等即视为本季未取证**——那是渲染时的活，不落盘。**准备包同样适用**（它虽落盘，但整包重生成、绝不就地改，见 [ADR 0013](docs/adr/0013-a-packet-is-regenerated-whole-never-patched.md)），渲染成带上季链接的 ⬜。

🔴 **前提句，不可省**（[#37](https://github.com/jiangxidong/EduApplication/issues/37) 补落）：**「取证」一直是本季的。** 上一季的取回不构成本季的取证——这正是换季降级存在的全部理由，也是「已落盘的 `✓` 只由换季降级改写」那句话的另一面（见「取证」与 [ADR 0007](docs/adr/0007-a-checkmark-is-earned-by-a-fetch-not-by-a-capability.md)）。此前这层季度相对性只能从两处推出来，从没被写成一句话，于是「⬜ = 尚未取证」与「那些节真相源里明明是 `✓`」看上去像冲突，实际不是。

**待核实清单** — `—`
工作区里全部带 `待核实` 标记的事实汇成的**派生视图**，出口是用户自查清单。不落盘。
与**缺口清单**的切分（哪一侧缺）见该词条。

**派生视图** — `—`
从真相源现算现打、**绝不落盘**的东西。
🔴 **v1 有哪些派生视图，归状态层契约 §1.5**（[ADR 0011](docs/adr/0011-the-glossary-defines-words-the-contract-holds-the-values.md)，[#42](https://github.com/jiangxidong/EduApplication/issues/42)）。这里原先存着一份比契约短七项的名单——**每新增一个视图就要跟着改的东西，本来就不该在词汇表里有第二份**。而「**X 是不是派生视图**」不去那份名单里读：它是 X 的**内涵**，写在 **X 自己的词条**里——每一个是派生视图的词，自己那条就这么写着。
⚠️ **本词条因此不举正例**（#42 定的引用配额判据）：agent 读这一条的时刻是「**我手上这行字该不该落盘**」，那一刻要的是判别式与**会判错的边界例**；「有哪些项」的消费时刻是「v1 要实现哪些视图」= 契约。下面三处保留的全是反例。
⚠️ **它的外延比「一张列得出来的表」宽。** 一道**闸口**的「过没过」、一件**发出即完成**的一次性产物，只要满足下面的判别式，同样是派生视图——「它不像一张表」不是判据，别拿它当反例（[#61](https://github.com/jiangxidong/EduApplication/issues/61)）。
⚠️ 反向也要分清：**闸口本身是一条规则，规则要落盘；派生的是它的判定结果，不是规则。**

判别式：**这行字为了保持为真，需不需要在别处改动时被改写？需要 → 它是镜像，不许存；不需要 → 它是事实或判断，留着。**

🔴 **前提句，不可省**（[#12](https://github.com/jiangxidong/EduApplication/issues/12)，由 [#30](https://github.com/jiangxidong/EduApplication/issues/30) 补落）：**「镜像」的前提是别处有源；没有源的可变内容是判断，不是镜像。** 少了这句，判别式会把 `recommenders.md` 的「主张 → 推荐人分配」和 `tier_basis` 一起判死——那两处都会被改写，但都**不是任何别处内容的镜像，它们就是真相源**（[#11](https://github.com/jiangxidong/EduApplication/issues/11)、#12 各判过一次）。此前本行只有单向的「不许存」半句，而前提句只写在状态层契约里，`main` 上读不到。

🔴 **限定句，同样不可省**（[#32](https://github.com/jiangxidong/EduApplication/issues/32)）：**镜像 ⇔ 这段内容存在一个唯一正确的取值，而那个取值在工作区内的别处。** 上一句管的是「有没有源」，这一句管的是「这段字有没有正确答案」——少了它，判别式对 `essays/canonical/` 是**逐字自毁**的：canonical 渲染物本来就是「把主张写成人话」的产物，长文整篇都在复述 `claims.md` 的主张，措辞一改就得跟着改，按字面读它整篇是镜像、该删。散文没有唯一正确取值（措辞是手艺），表格单元格有。⚠️ **不要读成「散文豁免」**——「不写素材几条、不写闸口几比几」禁的正是散文；切口开在有没有唯一正确取值上，不开在体裁上。**「工作区内」也是限定的一部分**：`✓ <url>` 指向站外，站外页面不是工作区的真相源。自测：两个 agent 各写一遍，差异算 bug 还是算文风？**算 bug ⇒ 是镜像。**

**一条关系边存在哪一端**（例如「哪篇文书用了哪些主张」写在文书 frontmatter 而不写在 `claims.md`）是**归属问题，归状态层契约**——按 [ADR 0011](docs/adr/0011-the-glossary-defines-words-the-contract-holds-the-values.md) 的消费方判据，agent 读它的时刻是「决定一条事实落进哪个文件」，不是「命名领域概念」。通则与逐条落位见 [ADR 0006](docs/adr/0006-claims-are-one-shared-truth-source.md) 的补充节。

**缺口** — `—`
一处**应有而实无**的空位。判别式是一条减法：**产品侧应有的集合 − 工作区侧实有的集合**，现算现打、**绝不落盘**——[#19](https://github.com/jiangxidong/EduApplication/issues/19) §6 点明材料缺口与 [#16](https://github.com/jiangxidong/EduApplication/issues/16) §8 的依据缺口**结构同构**，都是这条减法。落盘的缺口必然腐烂：有人往 `channels/` 补了事实，不会有人回来改 `tier_basis`。

六类，**解法两两不同**——这不构成拆成六个词条的理由：词条的单位是共享的判别式，不是动作（同「分档」五档、「待核实后缀」四种下一步）。真正的分界是**哪一侧缺**，它决定条目走哪份清单（见下条）：

| 侧 | 类型 | 差的是 | 解法 |
|---|---|---|---|
| 申请人 | 缺素材 | 这条主张没有素材支撑 | 补素材 |
| 申请人 | 缺人 | 有素材，但没有候选推荐人能证实 | 再找一位推荐人 |
| 申请人 | 缺放行 | 有素材、有人能证实，但素材 `sensitive = yes` 默认不进 support pack | 用户逐条放行（走「原文 → 拟用表述」成对确认） |
| 申请人 | 缺材料件 | **该渠道明说要**的某个槽位，在这所院校下还没有件 | 把件补进对应槽位 |
| 学校 | 依据缺口 | 该项目缺一条够格进 `tier_basis` 的 `✓` 事实 | 去官网查一个数字 |
| 学校 | 取证缺口 | why 段要引用的具名事实还不是 `✓`（`待核实` 的具名事实也不许进散文） | 去官网核这门课 / 这个人存不存在 |

⚠️ 学校侧两类**沿用「待核实后缀」的封闭词表分两栏，不新增后缀**：无后缀 = 去官网查，补上后分档可能升级；`官网未列` / `需邮件确认` / `口径不清` = 发邮件问，且多半问不到。一个项目的**全部**中段信号都落在后一栏时**不再当待办**——`reach` 是终态（见「分档」）。
⚠️ **「查不到第二条线」不是缺口**（[#22](https://github.com/jiangxidong/EduApplication/issues/22) §8）：第二条线**查得到就有、查不到就是真没有**，它不存在于任何非公开渠道，发邮件也问不到。放进任一栏都会在待办清单上挂一项**永远勾不掉**的事。
⚠️ **「这所学校根本不要」不是缺口**（[#19](https://github.com/jiangxidong/EduApplication/issues/19) §6）：材料缺口必须**逐渠道**现算并滤掉该渠道不要的件——Cornell 不收简历，`cv` 对 Cornell 就不是缺口。少这一维，产品会催用户准备一堆没人要的东西，其中一样（获奖证书）在 Cornell 还是违规的。
⚠️ `undecided` 行的 `tier_basis` **保持空**——不新增 `tier_gap` 列，不让一列承担两种语义。

**缺口清单** — `—`
「你还欠我这些」这个派生视图。**全局只有一份**，出口在冷启动阶段（[#15](https://github.com/jiangxidong/EduApplication/issues/15)），但**来源是多个**——文书、推荐信、材料都往里投递条目。

与**待核实清单**的切分是**哪一侧缺**：缺口清单只装**申请人侧**的缺口（上条表的前四类），**学校侧**的事实缺口一律归待核实清单（[#16](https://github.com/jiangxidong/EduApplication/issues/16)）。两者**各自是词条、各自都写着自己是派生视图**，本来就是并列的两个（[#42](https://github.com/jiangxidong/EduApplication/issues/42)：「X 是派生视图」住 X 自己的词条，不住「派生视图」词条）。

每个条目**三个属性**：

| 属性 | 是什么 |
|---|---|
| **对象指针** | 用户要去动的那一个东西（`claim_id` / `material` / 槽位 × 院校 × 渠道） |
| **缺什么类型** | 缺的是哪一类信息 |
| 🔴 **跳过的后果** | 不补这条，会失去什么 |

第三条是这个视图全部的价值所在：**「跳过这条，你这段就只能停在骨架」比「请补充」有用得多。**

⚠️ **对象指针一律挂到「用户要动的那一个东西」上，不挂到「缺的那个东西」上。** 典型：一条主张一个能证实它的推荐人都没有时，要动的是那条主张（换人 / 换主张 / 接受它进不了 pack），所以指针是 `claim_id`——挂 `recommender_id` 的话，这类缺口最该出现的时候指针恰恰是空的。这是 [ADR 0006](docs/adr/0006-claims-are-one-shared-truth-source.md) 用来杀掉「主张作为素材的一个字段」的同一条判别式：**必须能表示「零支撑的那一条」。**

⚠️ 有一类缺口的后果是**双向**的（放行敏感素材 = 第三方看到你的私事，不放行 = 材料泛泛）。**这类只陈述两边，agent 不倾向任何一边**（[#12](https://github.com/jiangxidong/EduApplication/issues/12)）。

---

## 文书

**canonical** — `canonical`
**内容层**：申请人这一侧的真相源。判别式是**既非派生视图、也非可再生产物**——它是别处推不出来的东西，所以删了补不回来。
（逐项目文书段住在这一层，但它没有「更大的一侧」可选——见下条，那是一条选择规则，不是本层的定义。）

**取信息量更大的一侧** — `—`
往 canonical 里存东西时的**选择规则**：完整长文、高分辨率逐页扫描、完整学历列表。**降级可逆、升级不可逆**，所以永远存大的那一侧。
只在同一份内容存在粗细两版时才适用；逐项目文书段这类只有一版的内容不涉及。

**渲染物** — `—`
canonical 的一种**形态**（长文 / 250 词版 / 可拆短答题的要点）。三者不是同一篇的长短，是三种形态。
**逐项目文书段不是渲染物**（见下），它既不开形态轴也不开版本轴。

**逐项目文书段** — `per-program`
「为什么是这个项目」这一段，是文书里**唯一必须逐项目手写**的内容，粒度对齐**提交单元**（[#18](https://github.com/jiangxidong/EduApplication/issues/18)）。
它由**两种假话**构成，因而受两套约束：讲学校的半句是**具名事实**（课程号、教授、实验室），受证据标记约束——散文只引用，事实本身落在申请渠道里；讲申请人的半句是**主张**（见「主张」词条），受素材门槛约束。
项目被 `dropped` 时**文件永不删**——排除不等于抹除。
闸门行为见 [#18](https://github.com/jiangxidong/EduApplication/issues/18)。

**版本** — `—`
文书**方向**（主线故事或目标定位）的一次变更。字数与格式的裁剪**不是**新版本（那是渲染物），跨校差异**也不是**新版本。旧版永不自动删。

**教练模式 / 代写模式** — `—`
两种模式**只差「谁产出文字」**，其余全部共用。教练模式的红线是**不写整段替代文字**。两者受同一道素材门槛约束，代写**不设解锁门槛**。

---

## 交付

**网申准备包** — `packet`
逐项目的一套产物，由 canonical 内容 × 该渠道的渲染规则生成，**可再生**——删掉能从真相源重建。它**停在提交前**。
形态：一个项目一个目录，一份按 portal 段落顺序从上到下走完的**提交动线** + 按该校渲染规则出的文书成品。
**总包不存在**（跨项目总览是派生视图）；**不拆「字段表」文件**（使用形态是左右并排对着抄，拆两份就要两处各存一遍答案）。

🔴 **整包重生成，绝不就地改**（[ADR 0013](docs/adr/0013-a-packet-is-regenerated-whole-never-patched.md)）：
包的 frontmatter 记 `source_fingerprint`（生成那一刻每个真相源的 `cksum`），进入准备包阶段先比对，
不一致就整包删了重生成。**在包里直接改内容，下次重生成时静默丢失。**
这也是包内允许出现镜像（交还点总表、缺口表）的唯一前提。

**包里不出现 `✓`，也不出现「待核实」这四个字**——那两个词是真相源的词汇，转述进包就是镜像。
二元标记在包里编码成**「有没有 ⬜」**：包开头声明一句「本包只渲染已取证的约束；凡真相源里尚未取证的，
一律以 ⬜ 出现」，此后全包零重复零歧义。说明层（引用块）豁免。
_Avoid_: 申请包、投递包、完成度计数（「已完成 12/17」是镜像）

**support pack** — `—`
给推荐人的一份支撑材料包：把「这个人要为我作证哪几条主张、各有哪些具体素材撑着」整理好交给推荐人，**由推荐人自己写信**。
🔴 **产品只做到 pack，不做成稿**（[#12](https://github.com/jiangxidong/EduApplication/issues/12) B 档）——成稿是推荐人的署名，代写它是伦理红线，不是能力问题。
**它是派生视图**：每次现算现打、绝不落盘。
粒度随**推荐人**走（见「推荐人」词条：一封信、往 N 个 portal 各交一次），因此它**不是逐项目的东西**；切分键与落位归状态层契约。
⚠️ 与**网申准备包**是两个包，别互相套用：那个逐项目、可再生、落盘；这个逐推荐人、派生、不落盘。
⚠️ 一条素材进不进得了这个包，判据是 **pack 门槛**（见下条），**不是素材门槛**。

**pack 门槛** — `—`
一条主张能不能进某位推荐人的 support pack。**两条合取**（[#12](https://github.com/jiangxidong/EduApplication/issues/12) §7，[#17](https://github.com/jiangxidong/EduApplication/issues/17) 结案后成两条）：
① 分配给这位推荐人的每条主张，至少有一条**他本人能证实**的素材；② 那条素材**不敏感**，或已被用户逐条放行。
单位是**主张**，不是素材篇数。**它是派生视图**：现算、绝不落盘。
🔴 **它与素材门槛是两道不同的闸，名字只差两个字。** 素材门槛管**成稿**（进了成稿的主张都得有素材撑着，**不问谁能证实**）；pack 门槛管**给第三方的包**（判定本体正是「谁能证实」）。
一条无人可证的素材**过得了素材门槛、过不了 pack 门槛**——这正是「由谁来说」词条里那句「同时不是合格的 support pack 素材」的完整形式。
两条合取各自的落盘字段归状态层契约（[ADR 0011](docs/adr/0011-the-glossary-defines-words-the-contract-holds-the-values.md)：词汇表定义词，契约持有取值；落盘形态变了而决策没变，见 [ADR 0014](docs/adr/0014-a-semantic-slot-is-not-a-stored-field.md)）。

**交还点** — `—`
准备包里 agent 停手、把控制权交回申请人的**那一段**。它是**复数**，不是终点线：
实测 Cornell 在第 1 / 4 / 8 段、UIUC 在第 1 / 6 / 8 / 9 段，登录在开头就要交还、推荐信在中间就要交还。
包里的体现是**逐段标记 + 开头一张交还点总表**（哪几段 / 为什么 / 不可逆吗）。
段标记四档：🟢 可直接粘贴 / 🛑 交还控制权 / ⬜ 缺 / ⛔ 本校明令不收。
_Avoid_: 终点线、最后一步、提交前检查点

**停手线** — `—`
产品明令不做的事，**三类十条**。

**动作类（六条）**：替用户登录、上传敏感文件、代签法律声明、付申请费、点提交、以推荐人名义发邀请。

**数据类（三条，[#17](https://github.com/jiangxidong/EduApplication/issues/17) 新增）**：不读取身份证件类文件的**内容**、不删除工作区的**任何文件**、不把工作区的任何内容带进**产品 repo**（含改写过的）。

**取证类（一条，[#20](https://github.com/jiangxidong/EduApplication/issues/20) 新增）**：**绝不由 agent 凭记忆产生 `✓` 行**。这是「绝不凭记忆报 deadline」的精确形式——约束挂在 `✓` 这个标记上，不挂在 deadline 这一个字段上，也不挂在「有没有联网」上。

⚠️ 三类各覆盖一面。动作类管动作；隐私是**四条独立的线**——读取 / 落盘 / 产物 / 仓库（**不上传 ≠ 不读取**），数据类三条补的正是「怎么存、读到哪、留多久」；取证类管的是**产物里的断言**，它是唯一一条在两个运行时上都只能靠产物形态执行、无法靠工具门控执行的红线。

**槽位** — `slot`
材料的预定义位置，**槽位先于文件**：槽位自带敏感级，所以 agent 从不需要「识别这个文件是什么」。不属于任何槽位的文件**只列文件名、不打开**。

v1 **七个**槽位（[#19](https://github.com/jiangxidong/EduApplication/issues/19)），本表只给中文名与标识符的对应：

| 中文 | 标识符 |
|---|---|
| 成绩单 | `transcript` |
| 评分说明 | `legend` |
| 翻译件 | `translation` |
| 学位证 | `degree` |
| 语言成绩单 | `english` |
| 简历 | `cv` |
| 身份类文件 | `identity` |

**目录形状（一个槽位 = 一个一级目录 = 一个敏感级）、哪些槽位逐院校、每个槽位的敏感级，全部归状态层契约 §1.3**——它们随路径结构变，是镜像。这里只有词。

槽位清单是**产品侧常量**（单向阀：仓库 → 工作区可流，反向绝不），不在工作区落定义文件，也**不预建空目录**。

槽位**不带「规格」这一维**——学校要什么规格（合并 / 分辨率 / 大小上限 / 收不收 Word）归**申请渠道**，槽位跨渠道而规格逐渠道，维度不对齐，两处各记一份必漂移。文件**自身**的属性（这份翻译件是否公证）记在文件旁的 `.md`：它答的是「这是什么」，不是「学校要什么」。

**禁读区** — `—`
agent **只做存在性与规格检查、不读内容**的槽位。判别式是「**读了也没用**」而不是「敏感」——`identity` 进禁读区，成绩单不进（要用来算 GPA）。v1 唯一的禁读槽位是 `identity`。

可执行界线（红线不能只是一句愿望）：**允许**只碰元数据的操作（`ls` / `stat` / `file` / `pdfinfo`——存在性、文件名、大小、格式、页数、尺寸）；**禁止**任何把文件内容送进上下文的动作（用 Read 工具读 PDF 或图片、`pdftotext`、OCR、截图、缩略图）。

🔴 **误读不可补救**：读进上下文的内容会落进运行时会话记录，那是工作区之外**产品删不掉**的第二份副本。别的红线越界了还能改，这一条不能。

**单向阀** — `—`
**两条**流向规则，方向不同、成因相同：**跨越那道边界的内容会被当成范本复制**。

**① 仓库 ↔ 工作区**：**仓库 → 工作区可流，反向绝不**。推论：样例档案必须**正向生成**，不得由真人数据反向脱敏——脱敏过的真人数据仍是真人数据。

**② `evals/` → `SKILL.md`**：**禁流**（[ADR 0010](docs/adr/0010-personas-ignite-assertions-they-are-not-examples.md)）。任何 `SKILL.md` 都不得引用 `evals/`。README 可以链过去——那是给人读的，不是给 agent 读的。
与「任何 `SKILL.md` 不许出现全景流程图」同一台机器：**agent 会照着它读到的东西产出**，所以内容进不进 `SKILL.md` 的判据不是「有没有用」，而是**照抄它会不会出事**。

**样例档案** — `persona`
一个**为点亮断言而正向构造**的申请人 + 一份自足的工作区快照。v1 **三个**，共用一个项目池。

判别式（[ADR 0010](docs/adr/0010-personas-ignite-assertions-they-are-not-examples.md)）：**一个维度能进它的轴集，当且仅当去掉这个维度，就有一条已承诺的断言永远不会被触发。** 与 [ADR 0005](docs/adr/0005-basis-points-at-an-existing-checked-fact.md) 给 `tier_basis` 定的准入测试同构——都拿「它挂在哪条已存在的东西上」代替「它像不像那么回事」。
⚠️ **像不像真人不是判据**。样例档案是**断言点火器**，不是示范形状；轴按断言选，不按真人原型列举。每个样例档案内部自洽，但组合是设计出来的。
文件头必带 `synthetic: true` + **生成它所依据的轴值组合**。判别式很硬：**写不出轴值组合的档案 = 它来自某个真人。**
_Avoid_: 虚拟档案、示例档案、fixture、三档档案（「档」是 `tier`）

**素材原文 vs 产物表述** — `—`
素材库存的是「父亲 2019 年确诊肝癌，化疗 8 个月，我休学照顾」，进文书的是「a family medical crisis in 2019」。**这是两个东西。**
规则挂在两者之间那道确认上，不挂在素材本身：**素材原文永不直接进任何产物**。按「敏感内容默认不外发」一刀切会把产品核心功能关掉——家庭变故正是文书里最常见的素材。
agent 在对话里向**用户本人**复述不受此限：产物线管的是产物，不是对话。

---

## 写入与归属

**阶段** — `stage`
产品流程的一段：冷启动 / 画像 / 选校 / 文书 / 材料 / 推荐信 / 准备包。它是**内容的自然切分**，也是**唯一写入者绑定的对象**。
阶段比 skill 细——四个 skill 是阶段的打包（见「skill」）。owner 绑阶段不绑 skill，因为 skill 的分组会因触发面调整而重排，阶段不会。

**skill** — `—`
分发与触发的单元，**粗于阶段**。v1 四个：`apply-us-master`（冷启动 + 画像）/ `pick-programs`（选校）/ `write-essays`（文书）/ `assemble-packet`（材料 + 推荐信 + 准备包）。
skill 表只做「阶段 → skill」的映射，**不出现任何路径字面量**——路径归属只有一份，在状态层契约里。

**入口** — `entrypoint`
四个 skill 里唯一宽 description 的那个（`apply-us-master`）。两个用途：**新开**（识别 / 初始化工作区）与**续上次**（读 `log.md` 判断该去哪）。
一个宽入口 + 三个窄专家是最抗撞车的形状——入口合并进选校会把选校的触发面撑宽，直接抢走「帮我改 SOP」。

**路由表** — `router`
`skills/<name>/SKILL.md` 里声明「何时读哪个 reference」的那部分。**每个 SKILL.md 是它自己 `references/` 的唯一路由表**，不存在全局路由文件。

**分发单元** — `—`
四个 skill 合起来的那一个整体。它们共用同一份工作区契约，**装一半就是坏的**，独立分发在语义上不成立。

**唯一写入者** — `owner`
一个**结构单元有且只有一个阶段**可以写它。跨 owner 的写一律禁止。

🔴 **绑定的单位是「该文件格式自己的结构单元」，不是「文件」**（[ADR 0008](docs/adr/0008-the-owner-binds-to-a-section-not-a-file.md)）：散文文件是**节**，表格是**列**，`documents/` 是**槽位**。一个文件有几个写入者不需要辩护——需要辩护的只有两件事，且都禁止：**一个结构单元有两个写入者**、**一条事实找不到写入者**。

切法必须来自契约里**预先声明、脚本能枚举**的结构，**不许临时发明切法**——否则「一个单元两个写入者」永远能靠切细来规避，规则归零（[#25](https://github.com/jiangxidong/EduApplication/issues/25)）。

归属判据是**消费方**：哪个阶段的决策依赖这条事实，哪个阶段就负责取证并落盘；多个阶段消费时归**最早**消费的那个，后到者只读。**消费不产生写入权。**

⚠️ **owner 可沿生命周期单向移交**：从阶段 A 交给 B 合法，条件是**任一时刻仍然唯一**且 **A 此后永不再写**（不可回流）。它与被否决的时序判据不同——时序拿「先填后填」当归属**依据**（循环），移交承认归属会**变**但要求变化**不可逆**。（哪些路径走移交、以什么为单位，见状态层契约。）

⚠️ **owner 只管会被改写的内容**：**append-only 且从不被改写**的内容**不在这条规则的辖区内**——不需要 owner，也不是破例。规则防的是**责任漂移**，而漂移必须**经由改写**发生。

⚠️ **owner 是一个阶段，不是一个 skill。** skill 的分组会因触发面调整而重排，阶段不会——绑阶段的全部意义就是重拆 skill 时归属一行不动。（哪个节归哪个阶段，见状态层契约。）
_Avoid_: 骨架 / 补全（用时序冒充责任——「什么时候被填」不能拿来定「谁该填」）

**节** — `section`
`channels/<channel_key>.md` 里的一个 `##` 标题段，**是 `channels/` 这类散文文件的结构单元**——也就是「唯一写入者」在这里绑定的对象。
⚠️ **它不是规则本身**：绑定单位是**该文件格式自己的结构单元**，散文文件才是「节」（表格是**列**，`documents/` 是**槽位**）。见「唯一写入者」词条与 [ADR 0008](docs/adr/0008-the-owner-binds-to-a-section-not-a-file.md) 限定 1。

节名取自一张**封闭词表**，**词表内禁止任何两项互为前缀**；标题**先剥掉行首所有不是汉字 / 拉丁字母 / 数字的字符，再按前缀匹配**——`## 🔴 推荐信机制 —— 风险 A：冒名发信` 合法，剥掉 `🔴 ` 后匹配到 `推荐信机制`；`## 材料上传 —— ⚠️ 与 Cornell 直接冲突` 合法，匹配到 `材料上传`。
后缀是**跨渠道冲突的现场记录**，行首那串非文字装饰（`🔴` / `⚠️` / `🔒`）是**全 repo 的书写习惯**——两者都不该为了机械匹配丢掉。判据因此让步而不是样例让步（[#28](https://github.com/jiangxidong/EduApplication/issues/28)）。🔴 剥离**绑 locale**（`LC_ALL=C` 下静默失效），实现与参考代码归状态层契约。

🔴 **v1 的词表本身、每个节的 owner 阶段与写入规则，全部归状态层契约**（[ADR 0011](docs/adr/0011-the-glossary-defines-words-the-contract-holds-the-values.md)）。理由是这两条都指向那边：节的顺序是 owner 分组的函数，owner 一变顺序就得改（**镜像**）；而 agent 从来不需要「全部十个节名」，它在写 `##` 标题那一刻要的是「**我这个阶段能写哪几个节**」——词表与 owner 列在使用现场不可分，拆开会造出一个没有任何消费者的产物。

⚠️ **节序已改为自由**（[#28](https://github.com/jiangxidong/EduApplication/issues/28)）：原「节按词表顺序排列」的规则与对应的孤儿检查已取消——它不定任何 owner，却要求后到的 owner **插入**而不是追加。上一段括号里「节的顺序是 owner 分组的函数」那个理由随之作废，**但结论不变**：词表与 owner 列在使用现场不可分，仍归契约。

**项目内容** — `—`
装 why 段引用的**具名事实**的那个法定节：课程号、教授、实验室、中心、项目特有课程结构。
它是**事实**不是约束，因此受证据标记约束；而「这段嵌在主文书里还是独立成题」是**规则**，归 `文书规格`。
🔴 why 段散文**只许引用标 `✓` 的具名事实**——`待核实` 在这里的含义是「我没能确认这门课 / 这个人存在」。

**项目级差异** — `—`
`channels/` 里 `## 项目级差异` 这一节。它在封闭词表内，但它是所有类别的**项目级覆盖层**，**不是又一个法定节**——它自己不带 owner。
它的层级结构与「同名节同一 owner，不论层级」这条归属规则，归状态层契约。
🔴 **「覆盖层」不蕴含「必须有东西可覆盖」**（[#28](https://github.com/jiangxidong/EduApplication/issues/28)）：`#### <法定节名>` 的合法性只由封闭词表决定，与同名 `##` 在该文件里是否存在无关——项目级事实（如 GRE 是否要求）本就可能渠道级说不出话。同票另定：`### <program_key>` 之下的**裸行非法**，每一行必须落在某个 `#### <法定节名>` 内（否则就是「一条事实找不到写入者」）。

---

## 明令避免的词

| 别用 | 用 | 为什么 |
|---|---|---|
| 保底 / 稳了 / safe | 较稳 / `safer` | safer 不是 safe，产品从不断言「会录」 |
| 录取概率 | （不输出） | 给申请人的数字化录取概率是红线 |
| 决定（作为一个列） | 状态 / `status` | 只有一个状态轴 |
| 申请单元 | 申请渠道 / `channel` | 它是通道，不是组织架构 |
| 保底校 / 目标校 / 冲刺校 | 项目 + 分档 | 校级门槛是谎——最小分叉粒度是项目 |
| 降级运行时 / 联网档位 | 取回失败 | 「降级」已指换季那件事；且产品里不存在「档位」这个实体（[ADR 0007](docs/adr/0007-a-checkmark-is-earned-by-a-fetch-not-by-a-capability.md)） |
| 骨架 / 补全（作为归属判据） | 唯一写入者 / 消费方判据 | 用时序冒充责任——「什么时候被填」定不了「谁该填」（[ADR 0008](docs/adr/0008-the-owner-binds-to-a-section-not-a-file.md)） |
| 匹配理由（作为一个列） | 依据 + 作废条件 | 它没有独立信息——「算冲刺，因为 X，除非 Y」就是完整的匹配理由；而它天生是散文，是 `|` 最高危的一列（[#11](https://github.com/jiangxidong/EduApplication/issues/11)） |
| 假保底 | 伪保底 / `pseudo_safer` | 中文名以词条为准；同一个概念在早期材料里两种写法并存（`docs/research/competitor-skills.md:39` 用的是「假保底」） |
| 三档虚拟档案 | 三个样例档案 / `persona` | 「档」已经是 `tier` 且是**五**档；同一个字指两个东西，「第二档档案在 UIUC 上应得 `safer` 档」当场有歧义 |
| 联网档位（作为回归模式名） | 离线模式 / 预取证模式 | 产品里不存在「档位」这个实体（[ADR 0007](docs/adr/0007-a-checkmark-is-earned-by-a-fetch-not-by-a-capability.md)）；模式是**回归套件**的属性，不是产品的属性 |
| 起步清单 / 推荐方向（作为首屏产出） | 缺口清单 / 第 0 问 | 首屏给不出可取证的方向建议——硬编码院校库在 Out of scope，凭记忆给撞停手线取证类（[ADR 0012](docs/adr/0012-the-first-reply-is-a-mirror-not-a-list.md)） |
| 已用于（作为素材的一个字段） | （不存，现推） | 它是镜像——文书侧由 canonical 跨校共用决定「跨校开头雷同」不是缺陷，推荐信侧的差异化轴是**主张**不是素材（[ADR 0006](docs/adr/0006-claims-are-one-shared-truth-source.md)、[#30](https://github.com/jiangxidong/EduApplication/issues/30)） |
| `usable_for`（作为素材的一个字段） | （不存）正文的「不能用在哪」 | 两条判据各判一次：选材路径是「主张 → 素材」，它不在任何决策链上；且文书类型清单逐渠道住在 `channels/` 的「文书规格」，**源在别处** = 镜像（[ADR 0014](docs/adr/0014-a-semantic-slot-is-not-a-stored-field.md)） |
| `concrete`（作为素材的一个字段） | （不存）正文三问 | 它是同一个文件正文的镜像，也是「零可再生缓存」禁的那样东西（[ADR 0014](docs/adr/0014-a-semantic-slot-is-not-a-stored-field.md)、[#30](https://github.com/jiangxidong/EduApplication/issues/30)） |
| 毕业目标（作为第 0 问） | 毕业去向意向 / `post_grad_intent` | 它是画像的一个字段，不是第 0 问——「回国」支不可取证（[#16](https://github.com/jiangxidong/EduApplication/issues/16) 排除），「留美」支能取证但取不全（[#34](https://github.com/jiangxidong/EduApplication/issues/34)：三所样例 1/3） |
