# 美国授课硕申请

这套 agent skills 的领域词汇表。域是**美国 coursework master（授课型硕士）申请**：从选校走到网申准备包、**停在提交前**，全部进度持久化在一个跨会话的工作区里。

本文件**只定义词**。文件长什么样归 [状态层的文件契约](https://github.com/jiangxidong/EduApplication/issues/4)，表怎么排版、措辞怎么写归 [选校推荐的输出契约](https://github.com/jiangxidong/EduApplication/issues/11)。实现细节一律不进这里。

## 三重身份

有落盘形态的术语，**中文名**（展示层，给用户看）与 **ASCII 标识符**（落盘层，被脚本聚合）必须分开写。没有落盘形态的，标识符写 `—`。

> 🔴 中文名**永不落进被聚合的列**：macOS 的 `uniq` 在中文下会数错、`sort -u` 会直接丢行，且与文件格式无关（[#4](https://github.com/jiangxidong/EduApplication/issues/4) §4.5 实测）。

---

## 工作区

**工作区** — `workspace`
一个申请人全部申请状态的**唯一权威目录**，由用户在会话开始时指定——产品不选路径。跨会话、跨设备续得上，全押在它上面。
_Avoid_: 项目目录、申请文件夹

**申请季** — `season`（形如 `2027fall`）
这一轮申请瞄准的入学学期。一个工作区**同时只属于一个申请季**。

**换季** — `—`
把工作区的 `season` 改到下一季这个动作。二申用户**不新开工作区**——画像、素材、旧文书、推荐人关系全部可复用，重开等于把核心资产扔掉。
换季只做两件事，**不清空任何内容**：全表证据降级（见「降级」）与状态回退（见「状态」）。

---

## 申请人

**申请人画像** — `profile`
申请人**结构化的、会被反复引用的、可填进表单的事实**：姓名拼写、学历、成绩、语言分、工作经历、目标与预算。
判别式：**能填进网申表单的格子，就属于画像。**
_不包含_：文书素材、文书草稿、成绩单扫描件——那些是叙事与原件，形态和用途都不同。
_Avoid_: 个人资料、背景、简历

**文书素材** — `material`
一段可被文书引用的真实经历。素材库是状态层的一等公民，不是文书的草稿纸。
每条素材带**敏感**标记（`敏感` / ASCII 二元，[#17](https://github.com/jiangxidong/EduApplication/issues/17)）：由 agent 提议、**用户可下调**。标 `yes` 的素材，其**原文永不直接进任何产物**——引用时须逐条确认「原文 → 拟用表述」。向用户本人复述不受此限。

**具体素材** — `—`
能答**三问**的素材：**什么时候** / **你做了什么动作** / **结果是什么**。三问答不全的是感想，不是素材。
⚠️ **「谁能证实」不在判据里**——可验证性是素材的独立属性（供推荐信选人用）。自学项目、个人作品无人可证，**照样是合格素材**。

**素材门槛** — `—`
「不许有无素材支撑的主张」这条**质量硬约束**。单位是**主张**不是篇数；3–5 个是参考值，不是闸门。
门槛拦的是**成稿**，不是模式：素材不足时代写模式照样能进，只是交付**带缺口标注的骨架 + 缺口清单**，而不是成稿。

---

## 项目与渠道

**项目** — `program` / 主键 `program_key`
一个具体的、可申请的硕士项目，粒度到 `学校 · 学院 · 项目名`。**最小分叉粒度是项目，不是学校**——同校不同院的门槛、表单、AI 政策都可能不同。

**申请渠道** — `channel` / 主键 `channel_key`
共用同一套 portal、申请费与上传规则的**通道**，通常落在学院或研究生院这一层。它是**通道**，不是组织架构——Cornell 的渠道是研究生院，并不等于某个学院。
_Avoid_: 申请单元

**项目池** — `program pool`
申请人在考虑的**全部**项目，一行一个项目。它是**唯一的项目实体**——不存在与之并列的第二张「清单」表。
_Avoid_: 选校表、候选池

**选校清单** — `—`
项目池里 `status ∈ {shortlist, applying, submitted}` 的**派生视图**。不落盘。

**投递名单** — `—`
项目池里 `status ∈ {applying, submitted}` 的**派生视图**。不落盘。

**状态** — `status`
项目在池子里的处境。**唯一的状态轴**——不存在并行的「决定」列。

| 中文 | 标识符 | 含义 | 换季后 |
|---|---|---|---|
| 在看 | `considering` | 进了池子，还在了解 | 保留 |
| 认真考虑 | `shortlist` | 在认真考虑，**还没拍板** | → `considering` |
| 已定投 | `applying` | **已拍板要投**，尚未提交 | → `considering` |
| 已提交 | `submitted` | 这一季已经交了 | → `considering` |
| 已排除 | `dropped` | 明确排除，**必须带一行理由** | **保留** |

- `shortlist → applying` 的切换点是**做出「投」这个决定的那一刻**，不是坐下来填表的那一刻。「开始填表」不产生状态变化。
- 换季时 `dropped` **保留**：它断言的是「这个项目不适合我」这个持续判断，不随季节失真；其余四个都在断言本季动作，跨季即失真。被保留的 `dropped` 理由进自查清单提示复核，但**不自动改状态**。
- `dropped`（明确排除过）与「从没进过池子」是两回事。

---

## 判断

**硬门槛** — `—`
项目**公开写明的**最低准入条件：最低 GPA、语言分下限、先修课、是否要 GRE。它是可核实的事实，因此是分档唯一合法的锚。

**分档** — `tier`
一个**顾问判断**，永远不带证据标记。四档**都以硬门槛为锚**：

| 中文 | 标识符 | 断言的是 |
|---|---|---|
| 冲刺 | `reach` | 你满足硬门槛，但拒你的主要原因会是**竞争**，而竞争数据不公开 |
| 匹配 | `match` | 你满足硬门槛，且有**公开信号**表明你落在它录取群体的中段 |
| 较稳 | `safer` | 你**明显超过**它公开写明的硬门槛，拒你的主要风险**不是你的条件** |
| 待判 | `undecided` | 上述三条都写不出依据 |

⚠️ `safer` **从不断言「会录」**，只断言「拒你的理由不会是你的条件」。凑不齐的档**不凑**。
「什么证据能合法支撑一个分档」归 [分档的「依据」到底能填什么](https://github.com/jiangxidong/EduApplication/issues/16)；本表只定义每一档**在断言什么**。
_Avoid_: 保底、稳了、safe

**录取率** — `—`
学校或项目**公布**的历史录取比例。它是**事实**，可标 `✓`，但**必须带口径**（哪一年、全体还是国际生、含不含 waitlist）。查不到口径就不裸给数字。

**录取概率** — `—`
「你有 N% 概率被录」。**不做**——任何形态、任何位置都不输出。

---

## 证据

**证据标记** — `evidence`
一个事实是否经过官网核实，**二元**：

- `✓ <官网链接>` — 已核实，事实取自这个 URL
- `待核实` — 其余一切情况

**待核实后缀** — `—`
`待核实` 可带一个括号后缀，说明**为什么还没核实**——「我没查」和「查了官网就是不写」对应的下一步动作完全不同。后缀取自**封闭词表**，不是自由散文：

| 后缀 | 含义 | 用户的下一步 |
|---|---|---|
| （无后缀） | 还没查 | 去官网查 |
| `官网未列` | 查过了，官网不写 | 查也没用，发邮件问 admissions |
| `口径不清` | 查到了，但年份/群体/含义不明 | 先确认口径才能用 |
| `需邮件确认` | 只能通过联系院校得到 | 直接发邮件 |

后缀**不参与机械汇总**（`待核实` 依然全中），只在生成自查清单时变成行动提示。

**降级** — `—`
换季时把 `✓ <链接>` 变成 `待核实（<上一季> 核过：<链接>）`。**必须保留上季链接**——抹掉就把「重新核实」变成「重新从零查」。

**派生视图** — `—`
从真相源现算现打、**绝不落盘**的东西：待核实清单、缺口清单、选校清单、投递名单、完成度自检、deadline 日历。
判别式：**这行字为了保持为真，需不需要被改写？需要 → 它是镜像，不许存。**

---

## 文书

**canonical** — `canonical`
**信息量最大的那一侧**内容：完整长文、高分辨率逐页扫描、完整学历列表。降级可逆、升级不可逆，所以永远存大的那一侧。

**渲染物** — `—`
canonical 的一种**形态**（长文 / 250 词版 / 可拆短答题的要点）。三者不是同一篇的长短，是三种形态。
**逐项目文书段不是渲染物**（见下），它既不开形态轴也不开版本轴。

**逐项目文书段** — `per-program`
「为什么是这个项目」这一段，是文书里**唯一必须逐项目手写**的内容，粒度对齐**提交单元**（[#18](https://github.com/jiangxidong/EduApplication/issues/18)）。
它有**两道闸防两种假话**：讲学校的半句走取证闸——具名事实（课程号、教授、实验室）必须**先带证据标记落到该渠道的项目级差异里**，散文只引用，未取证的具名事实**报错**；讲申请人的半句走素材门槛。
项目被 `dropped` 时**文件永不删**——排除不等于抹除。

**版本** — `—`
文书**方向**（主线故事或目标定位）的一次变更。字数与格式的裁剪**不是**新版本（那是渲染物），跨校差异**也不是**新版本。旧版永不自动删。

**教练模式 / 代写模式** — `—`
两种模式**只差「谁产出文字」**，其余全部共用。教练模式的红线是**不写整段替代文字**。两者受同一道素材门槛约束，代写**不设解锁门槛**。

---

## 交付

**网申准备包** — `packet`
逐项目的一套产物，由 canonical 内容 × 该渠道的渲染规则生成，**可再生**——删掉能从真相源重建。它**停在提交前**。
里面装什么归 [网申准备包的交付形态](https://github.com/jiangxidong/EduApplication/issues/8)。
_Avoid_: 申请包、投递包

**停手线** — `—`
产品明令不做的事，**两类九条**。

**动作类（六条）**：替用户登录、上传敏感文件、代签法律声明、付申请费、点提交、以推荐人名义发邀请。

**数据类（三条，[#17](https://github.com/jiangxidong/EduApplication/issues/17) 新增）**：不读取身份证件类文件的**内容**、不删除工作区的**任何文件**、不把工作区的任何内容带进**产品 repo**（含改写过的）。

⚠️ 动作类只覆盖动作这一面。隐私是**四条独立的线**——读取 / 落盘 / 产物 / 仓库：**不上传 ≠ 不读取**，数据类三条补的正是「怎么存、读到哪、留多久」。

**槽位** — `slot`
材料的预定义位置，**槽位先于文件**：槽位自带敏感级，所以 agent 从不需要「识别这个文件是什么」。不属于任何槽位的文件**只列文件名、不打开**。槽位清单归 [#19](https://github.com/jiangxidong/EduApplication/issues/19)。

**禁读区** — `—`
agent **只做存在性与规格检查、不读内容**的槽位。判别式是「**读了也没用**」而不是「敏感」——护照进禁读区，成绩单不进（要用来算 GPA）。

**单向阀** — `—`
仓库与工作区之间的流向规则：**仓库 → 工作区可流，反向绝不**。推论：测试用的虚拟档案必须**正向生成**，不得由真人数据反向脱敏——脱敏过的真人数据仍是真人数据。

**素材原文 vs 产物表述** — `—`
素材库存的是「父亲 2019 年确诊肝癌，化疗 8 个月，我休学照顾」，进文书的是「a family medical crisis in 2019」。**这是两个东西。**
规则挂在两者之间那道确认上，不挂在素材本身：**素材原文永不直接进任何产物**。按「敏感内容默认不外发」一刀切会把产品核心功能关掉——家庭变故正是文书里最常见的素材。
agent 在对话里向**用户本人**复述不受此限：产物线管的是产物，不是对话。

---

## 明令避免的词

| 别用 | 用 | 为什么 |
|---|---|---|
| 保底 / 稳了 / safe | 较稳 / `safer` | safer 不是 safe，产品从不断言「会录」 |
| 录取概率 | （不输出） | 给申请人的数字化录取概率是红线 |
| 决定（作为一个列） | 状态 / `status` | 只有一个状态轴 |
| 申请单元 | 申请渠道 / `channel` | 它是通道，不是组织架构 |
| 保底校 / 目标校 / 冲刺校 | 项目 + 分档 | 校级门槛是谎——最小分叉粒度是项目 |
