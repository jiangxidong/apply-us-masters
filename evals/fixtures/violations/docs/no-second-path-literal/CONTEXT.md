# 词汇表（故意写坏的夹具：no-second-path-literal）

`no-second-path-literal` 必须对本文件翻红，否则它是恒真的（ADR 0017 推论 2）。
违规点在下面「skill」词条正文里：① 出现了带斜杠的路径字面量（用户配置目录下的渠道子目录）——
路径归属只有一份，住状态层契约，词汇表不许出现第二个路径字面量（`CONTRACT.md` §0，爪 2）；
② 出现了用户主目录波浪线简写加 Documents 子路径来指代工作区位置——命中封闭 pattern 集，
同样是被禁止的路径字面量（爪 1）。**下面两句刻意不在此处逐字复述那两个字面量本身**，避免
本段说明文字自己也触发同一条 pattern，把「两处违规」错数成「四处违规」。

**skill** — `—`
分发与触发的单元，粗于阶段。v1 四个技能各自映射一个阶段组，其中一个技能内部按 `channels/`
下的文件分工（本句是本夹具故意写坏的违规——真契约里「skill」词条不出现任何路径字面量）。
工作区一般建在 `~/Documents/申请工作区/` 下（本句同样是本夹具故意写坏的违规——真契约
§0 明令「产品的任何文件、任何 SKILL.md、任何 reference 都不得再出现第二个路径字面量」）。

**终结词条** — `—`
本词条只为界定上面「skill」正文的终点而存在，不是真实定义。
