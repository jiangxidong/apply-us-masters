# 状态层文件契约（故意写坏的夹具：vocab-item-one-stage）

`vocab-item-one-stage` 必须对本文件翻红，否则它是恒真的（ADR 0017 推论 2）。
违规点：① 「硬约束」节 owner 列同时写了两个阶段——一个节只能有一个 owner（唯一写入者）；
② 「冻结点」节 owner 列留空——找不到写入者同样违规。

## 1.2 两个法定节（极小形状，仅供本夹具测试）

| 序 | 法定节 | 谁的决策依赖它 | owner 阶段 |
|---|---|---|---|
| 1 | `硬约束` | 选校 + 文书 | **选校**＋**文书** |
| 2 | `冻结点` | 准备包 |  |

**阴性对照**（合法，不应计入 FAIL）：真契约 `CONTRACT.md` §1.2 同名结构里，每一节 owner
列恰好含一个阶段 token（如「硬约束」在真契约里只填 **选校**），本夹具刻意把它改坏成两个。
