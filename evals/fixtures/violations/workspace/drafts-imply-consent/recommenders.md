# 推荐人（翻红夹具：drafts-imply-consent）

抄自 `sample-workspace/recommenders.md` 的「起草授权声明」节标题与表头，表内容清空
（保持「空表是当前的正确状态」这句话本身没错——错的是 `recommenders/drafts/` 已经建了）。

## 起草授权声明

> 本块是 `recommenders/drafts/<recommender_id>.md`（C 档推荐信草稿）的唯一前提。

**每推荐人一行，性质是事实，必须带日期**。日期是取得授权那天，不是写进本文件那天。

| recommender_id | 授权日期 |
|---|---|
