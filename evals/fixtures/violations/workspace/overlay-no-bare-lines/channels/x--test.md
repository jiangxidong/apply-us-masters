---
channel_key: x--test
season_downgraded:
  选校: 2027fall
  文书: 2027fall
---

# 渠道约束层（翻红夹具：overlay-no-bare-lines）

## 冻结点

- `待核实` 阴性对照：`##` 直下、遇到第一个 `###` 之前的行不判——这行属于 `冻结点` 本身，不在覆盖层内

## 项目级差异

### `x--test--prog`

- `待核实` 违规 1：裸行——`###` 之下、还没遇到任何 `####` 就出现的事实行，找不到写入者
> `待核实` 违规 2：裸行的引用块变体——`> ` 引用行同样算「行」，不豁免（豁免是证据标记扫描那条不同的规则）

#### 学历门槛

- `待核实` 阴性对照：这行正确落在 `#### 学历门槛` 内，不应计入 FAIL
