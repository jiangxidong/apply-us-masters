---
channel_key: x--test
season_downgraded:
  选校: 2027fall
---

# 渠道约束层（翻红夹具：section-prefix-match）

## 硬约束

- `待核实` 阴性对照：这是封闭词表内的合法节名，剥离/前缀匹配都应通过，不应计入 FAIL

## 英语门槛 —— 两档

- `待核实` 违规 1：`英语门槛` 不在 CONTRACT.md §1.2 的十节封闭词表内（ADR 0008 #65 已把它并入「学历门槛」，
  不作为独立节名存在）——剥离「## 」后前缀匹配不到任何 SECLIST 项，应翻红

## 项目级差异

### `x--test--prog`

#### 学位形态

- `待核实` 违规 2：`学位形态` 同样不在封闭词表内（#65 已把它按用途分流并入「费用与资格」与「项目内容」，
  不再是独立节名）——覆盖层内的 `####` 合法性同样只由封闭词表决定，应翻红
