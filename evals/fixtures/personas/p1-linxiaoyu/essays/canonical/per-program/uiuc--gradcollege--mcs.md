---
based_on: long.md
---

# Why UIUC MCS（虚构，`synthetic: true`）

> **逐项目 why-this-program 段，不可再生。** 无独立版本轴——改这篇不必开 `_versions/` 新版，
> 因为它本来就只有一份、且随 `channels/uiuc--gradcollege.md` 的取证状态变化而重写。
> 引用的具名事实必须先带 `✓` 落进 `../../../_shared/channels/uiuc--gradcollege.md` 的
> `#### 项目内容`——登记不是取证，`待核实` 的具名事实同样不许进本段（[#23](https://github.com/jiangxidong/EduApplication/issues/23)）。

（fixture 省略完整散文。以下句子引用了已落盘且带 `✓` 的具名事实，格式合规：）

MCS 的修业设计允许在**三个学期**内完成全日制学习（`Designed to be completed in as little as three semesters of full-time study`，
见 `../../../_shared/channels/uiuc--gradcollege.md` `#### 项目内容`）——这与我「留美工作 2-3 年后再看」的时间预算直接对齐，
不是泛泛的「贵校项目很强」。
