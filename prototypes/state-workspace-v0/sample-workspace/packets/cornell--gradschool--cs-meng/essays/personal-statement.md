---
rendered_from: essays/canonical/points.md
source_version: 1
render_rule: channels/cornell--gradschool.md § 文书规格
target_field: personal_statement
status: incomplete
---

# Personal Statement — Cornell MEng CS（渲染物）

⬜ **未完成。** 本校要的是与 SOP **分工不同的另一篇**，个人线。
`essays/canonical/points.md` 里可用的只有 **`## 个人背景 / 逆境`**（跨语言环境里把陌生任务拆成可执行步骤）；
**`## 多样性 / 贡献`** 与 **`## 职业目标`** 在 canonical 里就是空节 —— 那是素材缺口，不是本文件缺失。

⚠️ 按 `points.md` 自己的规矩，**节标题是自由文本、不是封闭词表**，所以这里按节标题对位，
不用 `P1`…`P4` 那套编号——那套编号在 `points.md` 里已经不存在了（#32）。
哪几节空着、缺的是什么，跑 `../../../derive-demo.sh` 现算，本文件不写计数。

**下一步在文书阶段，不在准备包阶段**：补素材 → 补 `points.md` 那两个空节 → 重新渲染本文件。
