# 网申准备包（可再生产物）

**每个项目一个目录**：`packets/<program_key>/`

**这一层是可再生的**：删掉整个 `packets/`，agent 应能从
`profile.md` + `essays/canonical/` + `documents/` + `channels/<channel_key>.md`
完整重建。**因此这里绝不允许出现任何别处没有的事实。**

包里装什么、用户怎么用它、完成度怎么自检
→ [#8 网申准备包的交付形态](https://github.com/jiangxidong/EduApplication/issues/8)。

**不预建空目录**：竞品坑 —— `taught-master` 的 examples 02–05 是空表头骨架，装了也没人能用。
目录里有什么，`ls` 一下就知道；不在这里记镜像状态。
